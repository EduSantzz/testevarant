name: Deploy para VM Local

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Atualizar site
        run: |
          cd /var/www/html/testevarant
          sudo git pull
          sudo systemctl restart nginx
